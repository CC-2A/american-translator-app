{
  "name": "us-translator",
  "version": "0.1.0",
  "private": true,
  "description": "Application mobile-first de traduction français / anglais américain pour voyageurs francophones aux États-Unis.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/app.js && node --check src/offlinePhrases.js && node --check src/offlineMatcher.js && node --check src/translationService.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
